#!/bin/sh
# This config file is for aliases and other config shared across zsh and bash

alias vi="vim"
set -o vi

# User specific aliases and functions
export P4EDITOR=vim
export EDITOR=vim
export VISUAL=vim

alias tmuxt="tmux a -t"
alias tmuxs="tmux new-session -s"
alias tmuxl="tmux list-sessions"

alias clip="nc -U ~/.clipper.sock"

function us102() {
   # ssh us102
   # ssh joshpfosi-home.sjc.aristanetworks.com

   # ssh -q joshpfosi@us102 "rm .clipper.sock";
   # rm -f .ssh/joshpfosi@us102:8378;
   # ssh -q us102

   mosh joshpfosi@us102
}

function us106() {
   # ssh us106
   # ssh joshpfosi-home.sjc.aristanetworks.com

   # ssh -q joshpfosi@us106 "rm .clipper.sock";
   # rm -f .ssh/joshpfosi@us106:8378;
   # ssh -q us106

   mosh joshpfosi@us106
}

export PATH=~/.vim:$PATH

if [ -e ~/.bashrc.arista ]
then
   source ~/.bashrc.arista
fi
